{
  "name": "bridge-guided-task-mode",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test test/*.test.mjs backend/*.test.js",
    "smoke:chrome": "npm run build && node scripts/chrome-smoke.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "@types/node": "^25.0.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.9.3"
  }
}
